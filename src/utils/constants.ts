const BURGER_API_URL = 'https://norma.nomoreparties.space/api';

const INGREDIENTS_URL = `${BURGER_API_URL}/ingredients`;
const ORDER_URL = `${BURGER_API_URL}/orders`;
const PASSWORD_RESET_URL = `${BURGER_API_URL}/password-reset`;
const PASSWORD_SAVE_URL = `${BURGER_API_URL}/password-reset/reset`;
const REGISTER_URL = `${BURGER_API_URL}/auth/register`;
const LOGIN_URL = `${BURGER_API_URL}/auth/login`;
const LOGOUT_URL = `${BURGER_API_URL}/auth/logout`;
const TOKEN_URL = `${BURGER_API_URL}/auth/token`;
const USER_URL = `${BURGER_API_URL}/auth/user`;

// const ANIMATION_DELAY = 300;

export {
  INGREDIENTS_URL,
  ORDER_URL,
  PASSWORD_RESET_URL,
  PASSWORD_SAVE_URL,
  REGISTER_URL,
  LOGIN_URL,
  LOGOUT_URL,
  TOKEN_URL,
  USER_URL,
}